{
  "name": "azure-pipelines-test",
  "private": true,
  "version": "0.0.0",
  "description": "Azure Pipelines Test",
  "scripts": {
    "test": "cross-env NODE_ENV=test mocha"
  },
  "dependencies": {
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "mocha": "^8.2.1"
  }
}
